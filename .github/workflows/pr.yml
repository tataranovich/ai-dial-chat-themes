name: PR Workflow

on:
  pull_request:
    branches: [development, release-*, github-deploy]

jobs:
  run_tests:
    uses: epam/ai-dial-ci/.github/workflows/generic_docker_pr.yml@1.9.4
    secrets: inherit
