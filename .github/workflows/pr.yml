# name: PR Workflow

# on:
#   pull_request:
#     branches: [development, release-*]

# jobs:
#   run_tests:
#     uses: alexey-ban/ai-dial-ci/.github/workflows/generic_docker_pr.yml@main
#     secrets: inherit
